"""CLI: DB aus Excel-Mockdaten und PFC-Dateien befuellen (Platzhalter).

Aufruf:
    python scripts/seed_from_excel.py --excel data/mockdaten.xlsm --db data/app.db --pfc-dir data/pfc

Umsetzung im Import-Arbeitspaket. Vorgabe: 06_excel_mockdaten_import.md.
Fehlende Werte werden als klar gekennzeichnete Default-Mockdaten erzeugt.
"""

# TODO(Import-AP): Argumente parsen und Import-Services aufrufen.
if __name__ == "__main__":
    raise SystemExit("Noch nicht implementiert (Import-Arbeitspaket).")
