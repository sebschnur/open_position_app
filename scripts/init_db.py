"""CLI: Datenbank-Schema erzeugen (Platzhalter).

Aufruf:  python scripts/init_db.py
Umsetzung in Arbeitspaket 2. Erzeugt data/app.db und die Tabellen, ohne eine
vorhandene DB stillschweigend zu ueberschreiben.
"""

# TODO(AP2): src.db.init_db aufrufen.
if __name__ == "__main__":
    raise SystemExit("Noch nicht implementiert (Arbeitspaket 2).")
